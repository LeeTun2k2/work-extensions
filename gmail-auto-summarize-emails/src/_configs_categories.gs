/**
 * Retrieves the predefined list of product categories and their critical criteria.
 * This function provides a consistent way to access the category definitions.
 *
 * @returns {Array<Object>} An array of category objects, each containing a 'name' and 'criticals' array.
 */
const getCategories = () => {
  return CATEGORIES;
}
